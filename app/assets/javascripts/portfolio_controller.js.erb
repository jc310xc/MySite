// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
$(document).ready(function(){
  var bgs = ["<%= asset_path 'dark_geometric.png' %>", "<%= asset_path '3px-tile.png' %>", "<%= asset_path 'dark_mosaic.png' %>"];
  var bgNames = ["Dark Geometric", "3px Tile", "Dark Mosaic"];

  console.log(bgs);

  var currbg = 0;
  $(document).on('keyup', function(e){
    if(e.keyCode == 40){
      currbg++;
      if(currbg == 3){
        currbg = 0;
      }

      alert(bgNames[currbg]);
      //$("body").css({"background-color" : "#FF0000"});
      $("body").css({"background-image": "url("+bgs[currbg]+")"});
    }
  });

  var ctx;

  function init() {
    ctx = $('#mazeCanvas')[0].getContext("2d");
    WIDTH = $("#mazeCanvas").width();
    HEIGHT = $("#mazeCanvas").height();

    ctx.fillStyle = "#BADA55";
    ctx.fillRect(100,100,10,10);
    ctx.fillRect(111,100,10,10);
    ctx.fillRect(122,100,10,10);
    ctx.fillRect(133,100,10,10);
    ctx.fillRect(144,100,10,10);
  }

  init();
});
